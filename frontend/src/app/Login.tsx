import{useState,type FormEvent}from'react';export function Login({onLogin}:{onLogin:(u:string,p:string)=>Promise<void>}){const[username,setUsername]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError('');setBusy(true);try{await onLogin(username,password)}catch(x){setError(x instanceof Error?x.message:String(x))}finally{setBusy(false)}}return <section className="login-shell"><form className="card login-card" onSubmit={submit}><div className="brand-mark">A</div><h1>ArtiSys ERP</h1><p>Gestão empresarial local, segura e independente.</p><label>Usuário<input data-testid="login-username" value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username" required/></label><label>Senha<input data-testid="login-password" type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" required/></label><button data-testid="login-submit" disabled={busy}>{busy?'Entrando…':'Entrar'}</button><div className="error" role="alert">{error}</div></form></section>}