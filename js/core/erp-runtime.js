'use strict';
const fs=require('node:fs');const path=require('node:path');
const {openDatabase}=require('./database/sqlite-database');const {runErpMigrations}=require('./database/migrations');const {createAuthService}=require('./auth/auth-service');const {createSettingsService}=require('./settings/settings-service');const {createBackupService}=require('./backup/backup-service');const {createSystemLogger}=require('./observability/system-logger');const {createSystemHealth}=require('./observability/system-health');const {createDiagnosticPackage}=require('./observability/diagnostic-package');
const {createContactService}=require('../domains/contacts/contact-service');const {createCatalogService}=require('../domains/catalog/catalog-service');const {createInventoryService}=require('../domains/inventory/inventory-service');const {createInventoryLogisticsService}=require('../domains/inventory/inventory-logistics-service');
const {createFinanceDimensionsService}=require('../domains/finance/finance-dimensions');const {createFinanceService}=require('../domains/finance/finance-service');const {createStatementImport}=require('../domains/finance/statement-import');const {createReconciliation}=require('../domains/finance/reconciliation');const {createRecurrenceService}=require('../domains/finance/recurrence');const {createFinanceAlertService}=require('../domains/finance/finance-alerts');const {createProcurementService}=require('../domains/procurement/procurement-service');const {createSalesAdminService}=require('../domains/sales-admin/sales-admin-service');const {createReportingService}=require('../domains/reports/reporting-service');const {createFinanceDocumentService}=require('../domains/reports/finance-document-service');const {createFinanceManagementService}=require('../domains/finance/finance-management');
function createErpRuntime({dbPath=':memory:',now=()=>new Date().toISOString(),idFactory}={}){const persistent=dbPath!==':memory:';const resolvedDbPath=persistent?path.resolve(dbPath):dbPath;if(persistent)fs.mkdirSync(path.dirname(resolvedDbPath),{recursive:true});const db=openDatabase(resolvedDbPath);runErpMigrations(db,now);const common={db,now};if(idFactory)common.idFactory=idFactory;const auth=createAuthService(common);const settings=createSettingsService({db,now});const contacts=createContactService(common);const catalog=createCatalogService(common);const inventory=createInventoryService({...common,catalog});const inventoryLogistics=createInventoryLogisticsService({...common,catalog,inventory});const financeDimensions=createFinanceDimensionsService(common);const finance=createFinanceService({...common,dimensions:financeDimensions});const procurement=createProcurementService({...common,contacts,catalog,inventory,finance});const salesAdmin=createSalesAdminService({...common,contacts,catalog,inventory,inventoryLogistics,finance});const reports=createReportingService({db,now});const financeDocuments=createFinanceDocumentService({db,finance,now});const financeManagement=createFinanceManagementService({db,finance,reports,now});const bankStatements=createStatementImport({...common,finance});const financeReconciliation=createReconciliation({...common,finance,statements:bankStatements});const financeRecurrences=createRecurrenceService({...common,finance});const financeAlerts=createFinanceAlertService({db,finance,financeManagement,settings,now});const logger=createSystemLogger({filePath:persistent?path.join(path.dirname(resolvedDbPath),'logs','erp.log'):null,now});const health=createSystemHealth({db,dbPath:resolvedDbPath,now});const diagnostics=createDiagnosticPackage({health,logger,now});const backup=persistent?createBackupService({db,dbPath:resolvedDbPath,backupDir:path.join(path.dirname(resolvedDbPath),'backups'),now}):null;logger.info('runtime.started',{persistent});let closed=false;return{db,auth,settings,contacts,catalog,inventory,inventoryLogistics,financeDimensions,finance,procurement,salesAdmin,reports,financeDocuments,financeManagement,bankStatements,financeReconciliation,financeRecurrences,financeAlerts,backup,logger,health,diagnostics,close(){if(closed)return;closed=true;logger.info('runtime.stopping');db.close();}};}
module.exports={createErpRuntime};
