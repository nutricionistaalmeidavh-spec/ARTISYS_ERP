'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');
const expected=['smoke','inventory-e2e','procurement-e2e','sales-admin-e2e','finance-management-base-e2e','finance-source-link-e2e','finance-dimensions-e2e','finance-base-idempotency-e2e','business-dashboard-e2e','dre-e2e','cashflow-e2e','period-comparison-e2e','cost-center-e2e','statement-ofx-e2e','statement-dedupe-e2e','reconciliation-payable-e2e','reconciliation-receivable-e2e','bank-transfer-e2e','finance-recurrence-e2e','recurrence-idempotency-e2e','financial-alerts-e2e','cash-projection-e2e'];
test('ERP QA registry contains only standalone release flows',()=>{const cfg=JSON.parse(fs.readFileSync('qa/artisys-qa.config.json','utf8'));assert.equal(cfg.systemId,'artisys-erp');const ids=cfg.flows.map(x=>x.id);assert.deepEqual([...ids].sort(),[...expected].sort());const critical=cfg.release.criticalFlows;for(const id of expected.filter(x=>x!=='smoke'))assert.ok(critical.includes(id),`${id} must be release critical`);for(const flow of cfg.flows){assert.ok(fs.existsSync(path.resolve(flow.file)),`${flow.file} missing`);const text=JSON.stringify(flow).toLowerCase();for(const word of ['checkout','restaurant','pizzeria','fiscal','cashier','self-service'])assert.ok(!text.includes(word),`${flow.id} contains ${word}`);}});
