export class ErpApi{baseUrl:string;token:string;constructor(baseUrl:string){this.baseUrl=String(baseUrl||'').replace(/\/$/,'');this.token=sessionStorage.getItem('erp-token')||'';}setToken(token:string){this.token=token||'';if(this.token)sessionStorage.setItem('erp-token',this.token);else sessionStorage.removeItem('erp-token');}async request<T=unknown>(path:string,{method='GET',body}:{method?:string;body?:unknown}={}):Promise<T>{const headers:Record<string,string>={Accept:'application/json'};if(this.token)headers.Authorization=`Bearer ${this.token}`;if(body!==undefined)headers['Content-Type']='application/json';const response=await fetch(`${this.baseUrl}${path}`,{method,headers,body:body===undefined?undefined:JSON.stringify(body)});const text=await response.text();let data:unknown=null;try{data=text?JSON.parse(text):null;}catch{data=text;}if(!response.ok)throw new Error(typeof data==='object'&&data&&'error'in data?String((data as {error:unknown}).error):`HTTP ${response.status}`);return data as T;}login(username:string,password:string){return this.request<{token:string;user:{name:string;role:string}}>('/api/v1/auth/login',{method:'POST',body:{username,password}});}logout(){return this.request('/api/v1/auth/logout',{method:'POST'}).finally(()=>this.setToken(''));}}