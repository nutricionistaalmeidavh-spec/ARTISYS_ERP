'use strict';
const {randomUUID}=require('node:crypto');const {writeAudit}=require('../../core/audit/audit-log');
function normalizeBusinessDate(value,{required=false}={}){if(value===null||value===undefined||value===''){if(required)throw new Error('Data de competencia obrigatoria.');return null;}const text=String(value).trim();if(!/^\d{4}-\d{2}-\d{2}$/.test(text))throw new Error('Data de competencia invalida.');const d=new Date(`${text}T00:00:00.000Z`);if(!Number.isFinite(d.getTime())||d.toISOString().slice(0,10)!==text)throw new Error('Data de competencia invalida.');return text;}
function createFinanceDimensionsService({db,now=()=>new Date().toISOString(),idFactory=p=>`${p}-${randomUUID()}`}={}){if(!db)throw new TypeError('Database is required.');const manager=actor=>{if(actor&&!['admin','manager','system'].includes(String(actor.role||'')))throw new Error('Autorizacao insuficiente.');};const bool=v=>v===false||v===0?0:1;const text=(v,l)=>{const s=String(v||'').trim();if(!s)throw new Error(`${l} obrigatorio.`);return s;};const mapGroup=r=>r&&({id:r.id,name:r.name,nature:r.nature,sortOrder:Number(r.sort_order),active:Boolean(r.active)});const mapCategory=r=>r&&({id:r.id,name:r.name,kind:r.kind,dreGroupId:r.dre_group_id,active:Boolean(r.active)});const mapCenter=r=>r&&({id:r.id,name:r.name,active:Boolean(r.active)});const mapDims=r=>r&&({entryId:r.entry_id,categoryId:r.category_id,costCenterId:r.cost_center_id,competencyDate:r.competency_date});function listDreGroups({includeInactive=false}={}){return db.prepare(`SELECT * FROM finance_dre_groups${includeInactive?'':' WHERE active=1'} ORDER BY sort_order,name`).all().map(mapGroup);}function saveDreGroup(input={},actor=null){manager(actor);const id=String(input.id||idFactory('dre')),name=text(input.name,'Nome'),nature=String(input.nature||'').toUpperCase();if(!['REVENUE','COST','EXPENSE','OTHER'].includes(nature))throw new Error('Natureza invalida.');const ts=now();db.prepare('INSERT INTO finance_dre_groups(id,name,nature,sort_order,active,created_at,updated_at) VALUES(?,?,?,?,?,?,?) ON CONFLICT(id) DO UPDATE SET name=excluded.name,nature=excluded.nature,sort_order=excluded.sort_order,active=excluded.active,updated_at=excluded.updated_at').run(id,name,nature,Number(input.sortOrder||0),bool(input.active),ts,ts);writeAudit(db,{action:'finance.dre-group.save',entity:'finance-dre-group',entityId:id,actor,context:{nature}},now);return mapGroup(db.prepare('SELECT * FROM finance_dre_groups WHERE id=?').get(id));}function listCategories({includeInactive=false}={}){return db.prepare(`SELECT * FROM financial_categories${includeInactive?'':' WHERE active=1'} ORDER BY name`).all().map(mapCategory);}function getCategory(id){return mapCategory(db.prepare('SELECT * FROM financial_categories WHERE id=?').get(String(id)));}function saveCategory(input={},actor=null){manager(actor);const id=String(input.id||idFactory('fcat')),name=text(input.name,'Nome'),kind=String(input.kind||'').toUpperCase();if(!['INCOME','EXPENSE','BOTH'].includes(kind))throw new Error('Tipo da categoria invalido.');const dreGroupId=String(input.dreGroupId||'').trim()||null;if(dreGroupId&&!db.prepare('SELECT 1 FROM finance_dre_groups WHERE id=? AND active=1').get(dreGroupId))throw new Error('Grupo DRE nao encontrado.');const ts=now();db.prepare('INSERT INTO financial_categories(id,name,kind,dre_group_id,active,created_at,updated_at) VALUES(?,?,?,?,?,?,?) ON CONFLICT(id) DO UPDATE SET name=excluded.name,kind=excluded.kind,dre_group_id=excluded.dre_group_id,active=excluded.active,updated_at=excluded.updated_at').run(id,name,kind,dreGroupId,bool(input.active),ts,ts);writeAudit(db,{action:'finance.category.save',entity:'financial-category',entityId:id,actor,context:{kind,dreGroupId}},now);return getCategory(id);}function listCostCenters({includeInactive=false}={}){return db.prepare(`SELECT * FROM cost_centers${includeInactive?'':' WHERE active=1'} ORDER BY name`).all().map(mapCenter);}function getCostCenter(id){return mapCenter(db.prepare('SELECT * FROM cost_centers WHERE id=?').get(String(id)));}function saveCostCenter(input={},actor=null){manager(actor);const id=String(input.id||idFactory('cc')),name=text(input.name,'Nome'),ts=now();db.prepare('INSERT INTO cost_centers(id,name,active,created_at,updated_at) VALUES(?,?,?,?,?) ON CONFLICT(id) DO UPDATE SET name=excluded.name,active=excluded.active,updated_at=excluded.updated_at').run(id,name,bool(input.active),ts,ts);writeAudit(db,{action:'finance.cost-center.save',entity:'cost-center',entityId:id,actor,context:{}},now);return getCostCenter(id);}function validateEntryDimensions(input={},entryKind=null){const categoryId=String(input.categoryId||'').trim()||null,costCenterId=String(input.costCenterId||'').trim()||null;if(categoryId){const category=db.prepare('SELECT * FROM financial_categories WHERE id=? AND active=1').get(categoryId);if(!category)throw new Error('Categoria financeira nao encontrada ou inativa.');if(entryKind==='PAYABLE'&&category.kind==='INCOME')throw new Error('Categoria de receita nao pode ser usada em conta a pagar.');if(entryKind==='RECEIVABLE'&&category.kind==='EXPENSE')throw new Error('Categoria de despesa nao pode ser usada em conta a receber.');}if(costCenterId&&!db.prepare('SELECT 1 FROM cost_centers WHERE id=? AND active=1').get(costCenterId))throw new Error('Centro de custo nao encontrado ou inativo.');return{categoryId,costCenterId,competencyDate:normalizeBusinessDate(input.competencyDate)};}function getEntryDimensions(entryId){return mapDims(db.prepare('SELECT * FROM financial_entry_dimensions WHERE entry_id=?').get(String(entryId)));}function setEntryDimensions(entryId,input={},actor=null){const entry=db.prepare('SELECT kind FROM financial_entries WHERE id=?').get(String(entryId));if(!entry)throw new Error('Lancamento financeiro nao encontrado.');const d=validateEntryDimensions(input,entry.kind),ts=now();db.prepare('INSERT INTO financial_entry_dimensions(entry_id,category_id,cost_center_id,competency_date,created_at,updated_at) VALUES(?,?,?,?,?,?) ON CONFLICT(entry_id) DO UPDATE SET category_id=excluded.category_id,cost_center_id=excluded.cost_center_id,competency_date=excluded.competency_date,updated_at=excluded.updated_at').run(String(entryId),d.categoryId,d.costCenterId,d.competencyDate,ts,ts);writeAudit(db,{action:'finance.dimensions.update',entity:'financial-entry',entityId:String(entryId),actor,context:d},now);return getEntryDimensions(entryId);}return{listDreGroups,saveDreGroup,listCategories,getCategory,saveCategory,listCostCenters,getCostCenter,saveCostCenter,validateEntryDimensions,getEntryDimensions,setEntryDimensions};}
module.exports={createFinanceDimensionsService,normalizeBusinessDate};
