# OBSOLETO — NÃO INTEGRAR

Esta branch (`feat/finance-imports-receipts-exports`) contém a implementação financeira antiga baseada na arquitetura anterior e ficou divergente da `main`.

Ela foi **substituída** pela compatibilização feita diretamente sobre a `main` e integrada pelo PR #5.

**Fonte canônica atual:** `main`.

Não fazer merge, rebase, cherry-pick ou iniciar novo desenvolvimento a partir desta branch. Use-a apenas como histórico. Consulte `docs/OBSOLESCENCIA.md` na `main`.
