'use strict';
const {json}=require('../http-utils');const {pathMatch,requireActor,body,asHttpError}=require('../router-utils');
function createProcurementRouter({runtime,sessions,bodyLimitBytes=1024*1024}={}){return async function(req,res,url){if(!url.pathname.startsWith('/api/v1/procurement/'))return false;try{const actor=requireActor(req,sessions);if(url.pathname==='/api/v1/procurement/orders'){if(req.method==='GET'){json(res,200,runtime.procurement.listPurchaseOrders({status:url.searchParams.get('status'),supplierId:url.searchParams.get('supplierId'),locationId:url.searchParams.get('locationId')}));return true;}if(req.method==='POST'){requireActor(req,sessions,['admin','manager']);json(res,201,runtime.procurement.createPurchaseOrder(await body(req,bodyLimitBytes),actor));return true;}}if(url.pathname==='/api/v1/procurement/receipts'&&req.method==='GET'){json(res,200,runtime.procurement.listReceipts({purchaseOrderId:url.searchParams.get('purchaseOrderId')}));return true;}let m;if((m=pathMatch(url.pathname,'/api/v1/procurement/orders/:id'))&&req.method==='GET'){const row=runtime.procurement.getPurchaseOrder(m.id);if(!row)throw new Error('Pedido de compra nao encontrado.');json(res,200,row);return true;}if((m=pathMatch(url.pathname,'/api/v1/procurement/orders/:id/submit'))&&req.method==='POST'){requireActor(req,sessions,['admin','manager']);json(res,200,runtime.procurement.submitPurchaseOrder(m.id,actor));return true;}if((m=pathMatch(url.pathname,'/api/v1/procurement/orders/:id/receive'))&&req.method==='POST'){requireActor(req,sessions,['admin','manager']);json(res,201,runtime.procurement.receivePurchaseOrder(m.id,await body(req,bodyLimitBytes),actor));return true;}return false;}catch(error){throw asHttpError(error);}};}
module.exports={createProcurementRouter};
