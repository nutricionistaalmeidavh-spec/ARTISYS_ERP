'use strict';
module.exports={id:'120-fiscal-core',up(db){db.exec(`
CREATE TABLE IF NOT EXISTS fiscal_company_settings(id TEXT PRIMARY KEY,company_id TEXT NOT NULL DEFAULT 'default',provider TEXT NOT NULL DEFAULT 'acbr-local' CHECK(provider IN('acbr-local','focus')),environment TEXT NOT NULL DEFAULT 'homologation' CHECK(environment IN('homologation','production')),cnpj TEXT NOT NULL,state_registration TEXT NOT NULL,legal_name TEXT NOT NULL,trade_name TEXT,crt TEXT NOT NULL,series_nfce TEXT NOT NULL DEFAULT '1',series_nfe TEXT NOT NULL DEFAULT '1',operation_nature TEXT NOT NULL DEFAULT 'VENDA',address_json TEXT NOT NULL DEFAULT '{}',updated_at TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS fiscal_profiles(id TEXT PRIMARY KEY,name TEXT NOT NULL,ncm TEXT NOT NULL,cest TEXT,cfop TEXT NOT NULL,origin TEXT NOT NULL,csosn TEXT,icms_cst TEXT,pis_cst TEXT NOT NULL,cofins_cst TEXT NOT NULL,unit TEXT NOT NULL,ibs_cbs_cst TEXT,c_class_trib TEXT,active INTEGER NOT NULL DEFAULT 1,created_at TEXT NOT NULL,updated_at TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS product_fiscal_data(product_id TEXT PRIMARY KEY REFERENCES products(id) ON DELETE CASCADE,fiscal_profile_id TEXT NOT NULL REFERENCES fiscal_profiles(id),gtin TEXT,overrides_json TEXT NOT NULL DEFAULT '{}',updated_at TEXT NOT NULL);
CREATE TABLE IF NOT EXISTS fiscal_sequences(document_type TEXT NOT NULL CHECK(document_type IN('nfce','nfe')),environment TEXT NOT NULL CHECK(environment IN('homologation','production')),series TEXT NOT NULL,next_number INTEGER NOT NULL CHECK(next_number>0),updated_at TEXT NOT NULL,PRIMARY KEY(document_type,environment,series));
CREATE TABLE IF NOT EXISTS fiscal_documents(id TEXT PRIMARY KEY,company_id TEXT NOT NULL DEFAULT 'default',source_type TEXT NOT NULL CHECK(source_type IN('POS_SALE','ADMIN_INVOICE')),source_id TEXT NOT NULL,document_type TEXT NOT NULL CHECK(document_type IN('nfce','nfe')),provider TEXT NOT NULL,environment TEXT NOT NULL,series TEXT NOT NULL,number INTEGER NOT NULL,status TEXT NOT NULL DEFAULT 'PENDING' CHECK(status IN('PENDING','PROCESSING','AUTHORIZED','REJECTED','UNKNOWN','FAILED','CANCELLED')),access_key TEXT,authorization_protocol TEXT,xml_text TEXT,cancellation_protocol TEXT,cancellation_xml_text TEXT,sefaz_code TEXT,sefaz_message TEXT,attempt_count INTEGER NOT NULL DEFAULT 0,idempotency_key TEXT NOT NULL UNIQUE,created_by TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL,UNIQUE(document_type,environment,series,number),UNIQUE(source_type,source_id,document_type));
CREATE TABLE IF NOT EXISTS fiscal_document_events(id TEXT PRIMARY KEY,fiscal_document_id TEXT NOT NULL REFERENCES fiscal_documents(id),event_type TEXT NOT NULL,status TEXT,metadata_json TEXT NOT NULL DEFAULT '{}',created_at TEXT NOT NULL);
CREATE INDEX IF NOT EXISTS idx_fiscal_docs_source ON fiscal_documents(source_type,source_id);
CREATE INDEX IF NOT EXISTS idx_fiscal_docs_status ON fiscal_documents(status,created_at);
`);}};