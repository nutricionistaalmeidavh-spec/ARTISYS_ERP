# OBSOLETO

Esta branch (`docs/erp-standalone-architecture`) é histórica e não deve ser usada como base de desenvolvimento, correção, merge ou release.

**Fonte canônica atual:** `main`.

A arquitetura e as decisões relevantes desta linha já foram sucedidas pela implementação consolidada na `main`. Consulte `docs/OBSOLESCENCIA.md` na `main` para o inventário oficial.
